Return plain text containing only intermodel relationship blocks. Do not write Markdown fences, bullets, numbering, comments, explanations, `ELEMENTS`, or `CONNECTIONS` headings.

Every block starts with exactly one model-pair header:
`[exact source model name -> exact target model name]`

Write each relationship belonging to that pair on its own following line:
`exact source element name connector exact target element name`

The connector must be exactly one of:
`play`
`plays`
`supports`
`empty`
`defines`
`is_responsible_for`
`performs`

A block may contain multiple relationship lines. Start a new header before relationships for a different model pair. Model names must exactly match model names present in the ADL input. Element names must exactly match existing element names or descriptions in their respective models. Do not include element types before element names. Do not use pipes, `::`, or any alternative syntax even if the parser accepts it. If no relationship exists, return exactly `NO_INTERMODEL_RELATIONSHIPS` on a single line and nothing else.

Create at most one relationship between any pair of elements. If multiple relationships connect the same source and target elements, keep only the single most specific applicable connector. Different connectors do not make a duplicate pair valid.
