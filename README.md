# AUM

1. go into the dir with the file ollama_thinking_ui.html
2. run python -m http.server 8000
3. new termin: ollama serve (if not already running)
4. go to http://localhost:8000/ollama_thinking_ui.html and write the prompt into the "Prompt" field 

